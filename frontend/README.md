# Credit Risk Predictor Frontend

React/Vite dashboard for the FastAPI credit risk predictor.

## Run

```bash
npm install
npm run dev
```

By default the app calls `http://127.0.0.1:8000`.

Create `.env.local` to use a different backend:

```text
VITE_API_BASE_URL=http://127.0.0.1:8000
```
