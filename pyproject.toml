[project]
name = "credit-risk-predictor"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "joblib",
    "scikit-learn",
    "openpyxl",
    "streamlit",
    "requests"
]

[tool.vercel]
entrypoint = "backend.main:app"
